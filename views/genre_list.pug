extends layout

block content
  h1= title
  
  ul
    //list all genres from database
    each genre in genre_list
      li 
        a(href=genre.url) #{genre.name} 
    else
      li There are no genres.
          //if there are no genres do this instead